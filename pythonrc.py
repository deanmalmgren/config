import os

# general path for code
pybraryPath = os.environ["HOME"] + "/Codes"

# particular codes
pyfigPath = pybraryPath + "/PyFig"
pygracePath = pybraryPath + "/PyGrace_Old"
pygracePackagePath = pybraryPath
tsDecompPath = pybraryPath + "/Timeseries_Decomposition/trunk"
toolsPath = pybraryPath + "/Tools"
pysqlPath = pybraryPath + "/PySQL/trunk"
